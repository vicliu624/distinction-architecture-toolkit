# Explicit Architecture VSCode Extension

Initial commands:

- Explain Selected Code
- Find Responsibility Overload
- Generate Surface Inventory

This first version intentionally outputs Markdown rather than a complex WebView. The WebView can come after the architecture model stabilizes.
